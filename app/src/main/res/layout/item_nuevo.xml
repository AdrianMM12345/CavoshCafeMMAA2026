<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:padding="8dp">

    <ImageView
        android:id="@+id/tvNombreProducto"
        android:layout_width="128dp"
        android:layout_height="128dp"
        android:padding="8dp"
        android:src="@drawable/sh_bg_item_light_silver"
        android:contentDescription="content_description"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"/>
    <ImageView
        android:id="@+id/ivFavorito"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:layout_marginTop="90dp"
        android:background="@drawable/sh_notificacion_on"
        android:padding="8dp"
        android:contentDescription="content_description"
        android:src="@drawable/ic_favoritos"
        app:tint="@color/white"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <TextView
        android:id="@+id/tvNombreProducto"
        android:layout_width="128dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="caramel machiato"
        android:textColor="@color/black"
        android:textSize="16sp"
        android:textStyle="bold"
        app:layout_constraintTop_toBottomOf="@id/tvNombreProducto"
        app:layout_constraintTop_toBottomStart="parent"/>
            />
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="$ 4.00"
        android:textColor="@color/carmine_pink"
        android:textStyle="bold"
        app:layout_constraintTop_toBottomOf="@id/tvPrecioProducto"
        app:layout_constraintStart_toStartOf="parent"/>

    <ImageButton
        android:id="@+id/btnAgregar"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:layout_alignParentEnd="true"
        android:background="@drawable/sh_notificacion_on"
        android:src="@drawable/ic_add"
        app:tint="@color/white" />




</androidx.constraintlayout.widget.ConstraintLayout>